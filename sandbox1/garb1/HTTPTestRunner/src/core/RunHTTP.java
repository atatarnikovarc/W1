package core;

public class RunHTTP {

	/**
	 * @param args
	 */
	/*public static void main(String[] args) {
		HashMap testResult = new HashMap();
		while (true) {
			HashMap testScript = readNextTestScript();
			HashMap testScriptResult = executeTestScript(testScript);
			testResult.add(testScriptResult);
		}
		writeTestResultToDisk(testResult);
	}
	
	public void writeTestResultToDisk(HashMap result) {
		File resultFile = new File();
		HashMap testResult;
		while (true) {
			testResult = new HashMap(result.getNext());
			writeTestResultFolder(testResult);
			writeTestStatusEntry(resultFile, testResult);
		}
	}
	
	public HashMap readNextTestScript() {
		
	}
	
	public HashMap executeTestScript(HashMap script) {
		HashMap result = new HashMap();
		while (true) {
			HashMap step = getNextStep(script);
			HashMap stepResult = executeStep(step);
			result.add(stepResult);
		}
		return result;
	}

	*/
}
