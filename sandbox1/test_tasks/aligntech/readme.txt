На python c помощью информации из этих двух источников (в любом формате) посчитать,
у какой станции метро больше всего остановок (в радиусе 0.5 км).

deps to run:

sudo pip install xlrd
sudo pip install geopy

todos--
use different input data format(not xls or use different lib to read data)
use lambda in forming narrow_coord_list
use threads to process


33 Юго-Западная, вход-выход 3 в южный вестибюль
Time taken: 643.027032137