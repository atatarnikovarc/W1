На python c помощью информации из этих двух источников (в любом формате) посчитать,
у какой станции метро больше всего остановок (в радиусе 0.5 км).

deps to run:

sudo pip install openpyxl
sudo pip install geopy

todos--
presort surf_coords list
convert unicode code into human readable
use threads to process